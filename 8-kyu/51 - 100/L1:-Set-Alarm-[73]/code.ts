export const setAlarm = (employed: boolean, vacation: boolean) => (employed === true) && (vacation === false);

/*
export function setAlarm(employed: boolean, vacation: boolean) {
  return employed && !vacation
}
*/
